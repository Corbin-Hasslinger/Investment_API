[project]
name = "investment_api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Corbin Hasslinger", email = "corbi@ad.unc.edu" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "pyright>=1.1.411",
    "pytest>=9.1.1",
    "ruff>=0.16.0",
    "uvicorn>=0.52.0",
]

[project.scripts]
investmentapi = "investment_api:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
