[project]
name = "atlas_api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Corbin Hasslinger", email = "corbi@ad.unc.edu" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.52.0",
]

[build-system]
requires = ["hatchling>=1.13.0,<1.14.0"]    
build-backend = "hatchling.build"
